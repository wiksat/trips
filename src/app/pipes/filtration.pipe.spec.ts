import { FiltrationPipe } from './filtration.pipe';

describe('FiltrationPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrationPipe();
    expect(pipe).toBeTruthy();
  });
});
